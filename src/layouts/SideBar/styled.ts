import { Divider, styled, Typography } from '@mui/material';

export const SideBarDivider = styled(Divider)({
  background: '#BDBDBD',
  marginTop: '27px',
  width: '240px',
  border: '0,5px',
});
export const CompanyName = styled(Typography)({ marginTop: '20px', height: '40px' });
